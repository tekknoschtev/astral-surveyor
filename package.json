{
  "name": "astral-surveyor",
  "version": "1.0.0",
  "description": "A chill, tranquil space exploration game",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "node -e \"const fs=require('fs'); const path=require('path'); fs.mkdirSync('dist', {recursive:true}); fs.copyFileSync('game.html', 'dist/index.html'); fs.cpSync('css', 'dist/css', {recursive:true});\"",
    "serve": "node -e \"const http=require('http'); const fs=require('fs'); const path=require('path'); const url=require('url'); const mime={'html':'text/html','js':'application/javascript','css':'text/css','png':'image/png','jpg':'image/jpeg','svg':'image/svg+xml'}; const server=http.createServer((req,res)=>{const parsedUrl=url.parse(req.url); let filePath=parsedUrl.pathname; if(filePath==='/' || parsedUrl.search){filePath='index.html';} const file=path.join('dist',filePath); if(!fs.existsSync(file)){res.writeHead(404);res.end('Not found');return;} const ext=path.extname(file).slice(1); res.writeHead(200,{'Content-Type':mime[ext]||'text/plain','Access-Control-Allow-Origin':'*'}); fs.createReadStream(file).pipe(res);}); server.listen(3000,()=>console.log('🚀 Server running at http://localhost:3000\\n📁 Serving dist/ directory\\n🔄 Run npm run build to update files'));\"",
    "dev": "tsc --watch",
    "clean": "node -e \"const fs=require('fs'); try{fs.rmSync('dist',{recursive:true,force:true})}catch(e){} console.log('Cleaned dist directory')\"",
    "rebuild": "npm run clean && npm run build",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check src/**/*.ts",
    "test": "npm run build && vitest run",
    "test:watch": "npm run build && vitest",
    "test:coverage": "npm run build && vitest run --coverage",
    "test:ui": "npm run build && vitest --ui"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@types/node": "^24.2.1",
    "@vitest/coverage-v8": "^2.0.5",
    "@vitest/ui": "^2.0.5",
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "jsdom": "^25.0.0",
    "prettier": "^3.6.2",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.39.0",
    "vitest": "^2.0.5"
  },
  "keywords": [
    "game",
    "space",
    "exploration",
    "browser",
    "html5"
  ],
  "author": "Astral Surveyor Team",
  "license": "MIT"
}
